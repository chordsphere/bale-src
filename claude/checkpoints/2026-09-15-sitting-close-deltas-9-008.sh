#!/usr/bin/env bash
# Blind checkpoint — sitting close, continue-plan-004 deltas to claude/MASTER.md.
# Authored at the sitting from the brief's own bytes. Runs in staging.
# Outcome: every landing paragraph present once (whitespace-collapsed),
# the header edited in place, the section skeleton intact. Exit 0/1/2.
set -u
command -v python3 >/dev/null 2>&1 || { echo "[SKIP] python3 not found"; exit 2; }
[ -f claude/MASTER.md ] || { echo "[FAIL] claude/MASTER.md absent"; exit 2; }
python3 - <<'PY'
import re, sys
doc=open("claude/MASTER.md", encoding="utf-8").read()
c=re.sub(r"\s+"," ",doc)
SID=r"2026-\d\d-\d\d-sitting-close-deltas-9-\d{3}"
pins=[["A", "Landed 2026-09-15, the continue-plan-004 sitting (master `2026-09-15-continue-plan-004`, read-only; opened 2026-09-15 UTC on \"continue the plan\" with two additions — outward docs three months stale and a model-agnostic bale, and a bare `bale apply` that opens every tarball in Downloads; the previous master `2026-09-15-continue-plan-001` closed at close-8). Three rows opened and landed in one sitting, three more opened and queued; this block carries only what has no row home; close recorded by `<your-sid>`:"], ["A", "- Wave record: three sessions. `2026-09-15-board-90-doc-residue-005` (bumpless, doc lane with one `bin/bale` string) and `2026-09-15-board-89-decline-cause-006` (0.4.31) ran beside each other on disjoint forecasts and applied clean, each with a blind checkpoint of eleven outcomes rehearsed both ways before emission. `2026-09-15-board-101-bare-apply-cap-007` (0.4.32) is a re-attempt: the first attempt was HELD with the blind checkpoint seven of seven PASS and the worker's own validation exiting 1 on a needle it could never see (§6 entry 138); the re-attempt's change set was byte-identical. One out-of-forecast drift admitted at 101 (`bin/bale_report.py`, the handoff branch of the checkpoint-scope refusal — the ruling could not be met without it), and seven `tests/` paths admitted at 89 that the desk should have forecast (§6 entry 140). - Sitting-open record: close-8's notes.md ratified wholesale at the open (bullet spacing per the doc, unbracketed growths, block E under the 09-14 preamble, the coincidental duplicate paragraph, block G's five bullets). The sitting-open version check was satisfied by the request's provenance stamp, 0.4.30. Each worker's notes.md ratified in-sitting; the calls are named on the rows. - The bare-apply finding, for the record: the resolver globbed `*.tar.gz` and opened every match with `getmembers()`, which decompresses the whole gzip stream to find `manifest.json`; cost per bare run was the total bytes of every tarball in every search path. The operator's rule, VERBATIM: \"I should only ever be bare applying the latest tarball or second latest that fits the conventions in my search paths, it doesn't need to look through more than that.\" Ruled and landed at row 101 (§5). - Two desk misses of record, both sequencing-side: row 47 dropped out of the \"sequencing for the next desk\" line at the 09-14 close when row 74 was decoupled from it, and stayed out until the operator asked whether the HOLD paste block was still queued (§6 entry 136); and row 95's two handoff-gate test failures were re-found from board 89's notes and dispatched to 101 as a rider without the desk recognizing that row 95 already held them (§6 entry 137). Row 95 is DONE by 101; the dispatch check now reads the board's open rows by touched file, not only the registry (§7). - Board 89's worker asked one non-blocking question in chat before building and read the operator's \"go\" as no preference — the third light-tier specimen for row 96, and a correct reading of a detail call under docs that still have no tier for it. - Ratification debt carried forward, per convention: THIS close's notes.md queues to the next sitting's open. - Board deltas of the close: rows 89, 90, 101 opened and DONE; row 95 DONE by 101; rows 99, 100, 102, 103 opened in §4; rows 47, 77 grown; evidence entries 136–140 in §6; four contracts dated 2026-09-15 in §5; the version landmark 0.4.32, the tests-forecast rule, the re-attempt closing line, and the board-by-file dispatch check in §7. - Registry deltas of the close: consumed — `persist_pack_session`'s stale docstring (89) and the BALE.md §7/§7.2 includes-as-scope true-up (closed on 90's read: nothing to fix). New entries from the three workers' Proposals: the `bale config hooks` row in BALE.md §5's command table and the \"every admission y/N names its cause\" sentence (both ride 99b); the `bin/bale_apply.py` module docstring that denies the lazy `import bale_pack` it contains; `shlex.quote` on the non-TTY bare-apply refusal path (rides row 102); \"request includes\" in the shared blindness diagnosis (only if the byte-shared-diagnosis constraint is re-ratified). Board 89's `<path>`-remedy proposal was folded into 101 and landed as the quoted alternative beneath the decline line. - Sequencing for the next desk: 91 and 95 were beside each other; 95 is done, so 91 → 47 → 102 → 96 → 97 → 99a → 103 → the 100 arc (design sitting first; absorbs 77; 99b rides its doc wave) → the held wave: 68, then 56+57 beside it, then 69, then 37 → 43 → 45 → S6. Rows 92, 93, and 98 stand until brought forward."], ["B", "99. **Outward-facing doc refresh** — queued 2026-09-15 (docs; two sessions on one seam). The root `README.md` is dated 2026-06-03 and describes a different tool: \"four docs in docs/\" (five since PLANNER.md), two `bin/` modules (twelve), a daily-use section with no `open`, `relay`, `handoff`, `stats`, `rollback`, `config hooks`, checkpoints, bundles, or bare apply — and it ships in the release tarball, so a fresh install reads it first. Operator's scope ruling at the sitting: README, the `--help` epilogs and `config init` wizard text, and a BALE.md true-up. **99a**: `README.md` plus the help and wizard strings (`bin/bale`, `bin/bale_config.py`); doc lane with string riders. **99b**: the BALE.md true-up, its own session (the doc is ~260 KB), riding the 100 arc's doc-sweep wave, and carrying the two registry sentences that name it. `tests/test_readme_identity.py` pins `--readme-file` behavior, not README content."], ["B", "100. **Model-agnostic bale — ARC** — queued 2026-09-15. Operator's goal, VERBATIM: \"I want to abstract everything away from claude or anthropic specifically so that I can cleanly insert a different model.\" The coupling has four layers with different costs: (1) branding prose — \"Claude\" as the worker's name in the five globals (~250 hits), `bin/bale` docstrings, report next-step lines, README; (2) the `--expects-probe claude-decides` enum, pinned in two schemas and every telemetry record; (3) the `CLAUDE.md` file name, one of `GLOBAL_DOCS` and the name the pack opener tells the worker to read first; (4) the `claude/` project directory — `[validation] base`, `[archive]`, `base/\"claude\"/\"telemetry\"` hardcoded in stats, ADR paths, every existing telemetry record. Also: CLAUDE.md §11 carries claude.ai-surface facts (auto-compaction, \"press Continue\", tool-use limits) that belong in a surface-notes subsection. Shape: a read-only design sitting first (the worker noun; how deep — layers 1–2, +3, +4, decided there by operator ruling; compatibility — enum aliases, a config key for the directory defaulting to the old spelling), then a wave: doc sweep + guard patterns, code strings + help text, the compatibility layer if 3–4 are in. Row 77 (inject→carry) rides the arc; row 97's ADR spelling is ratified relative to whatever the directory is called. Row 103's findings feed the doc sweep, so 103 runs before the arc."], ["B", "101. **Bare `bale apply` bounded resolution — DONE 2026-09-15** at `2026-09-15-board-101-bare-apply-cap-007`, 0.4.32 (re-attempt; the first attempt HELD on the worker's own validation needle, change set byte-identical). The three rules landed (§5): only `response-*.tar.gz` is ever opened; the two newest by `st_mtime_ns` are examined, one cutoff so a tie at either rank is visible in full; the no-candidate refusal names each examined file with its reason and counts the unexamined older ones. Also landed: the decline's quoted alternative line when the other examined file answers an open session (board 89's `<path>` proposal, folded here); the apply parser description and the positional's \"an open session\"; both row-95 handoff-gate cases rewritten to the ratified contracts (read-includes phrase with a read-side remedy; the empty plan inherits the parent's forecast) with the remedy split in `bin/bale_report.py` (admitted drift). Calls ratified: un-named files uncounted in the refusal; `BARE_APPLY_EXAMINE_CAP = 2` as a module constant; \"re-bail\" as the read-side remedy's verb; the diagnosis kept byte-shared across callers."], ["B", "102. **Explicit-name tarball resolution** — queued 2026-09-15 (small; `bin/bale_apply.py`, retry's argument path, BALE.md a sentence). A tarball argument with no directory component that does not exist relative to cwd is looked up in `apply.search_paths`, for `apply <name>` and `retry <name>` alike — a lookup of the name given, never the bare search (the operator is content that `retry` does not bare-search). With no exact match, refuse and list near-names — `response-<sid>*.tar.gz`, the browser's `(1)` variants — as complete quoted lines. Carries the `shlex.quote` registry entry for the non-TTY refusal path. Motivation, from the operator: typing the whole path is annoying, and a second download of the same name gets `(1)` appended, so the quotes are load-bearing."], ["B", "103. **Cross-model capability probe** — queued 2026-09-15 (a design sitting, then one pack run N ways). Operator's goal, VERBATIM: \"queue a session where I run the same test pack (intentionally tasked to extract model capabilities) with both fable 5 and sonnet 5 (or also an open weight model) so that we can compare and get a deeper understanding of what lesser models miss, and determine if we need to edit or improve any documentation or procedures to allow weaker models to not trip up as much.\" Shape: one request built to exercise the protocol's tripwires (reading path, verbatim landings, blind-checkpoint posture, both-branch validation rehearsal, notes.md shape, clarification-versus-detail-call judgment, drift enumeration, the crafter, chat-versus-exchange), packed once per model with identical argv so each gets its own session and otherwise identical bytes; each response through its own session's gate (`bale validate`, then apply or HOLD), graded by the same blind checkpoint; `cost.model_tier` per attempt is the telemetry home. Output: a findings report and doc/procedure deltas for weaker models. Runs after 99a and before the 100 arc's design sitting so the findings feed the sweep. The desk writes the probe request at the design sitting."], ["C", "[2026-09-15: resequenced — this row dropped out of the sequencing line at the 09-14 close when row 74 was decoupled from it (§6 entry 136) and is back directly after row 91. Fresh specimen: 101's HOLD this sitting, relayed by hand as card plus log. The composed retry line on the banner is quoted through `shlex.quote` per the re-attempt closing-line rule (§7).]"], ["C", "[2026-09-15: rides the 100 arc — the same sweep shape over the same files.]"], ["C", "[2026-09-15: DONE at `2026-09-15-board-89-decline-cause-006`, 0.4.31. Five prompts switched to `confirm_yn_decision` with literal three-line tables per prompt and one renderer (`bale_report.format_decline_line`); the drift line carries every typed value (`[*allow_norm, *drift_paths]`); `format_config_hooks_json` moved beside `format_status_json`; the `persist_pack_session` docstring rider taken. Calls ratified: the sweep's unreachable empty-answer line kept for parity; the piped supersession path's cause-less line; tables keyed by string literals with an `ast` parity test against `bin/bale`'s constants; `ValueError` on renderer misuse; the wizard's two `confirm_yn` uses left. Seven `tests/` paths admitted out-of-forecast (§6 entry 140). Its `<path>`-remedy proposal folded into 101; its BALE.md sentence rides 99b; its handoff-gate finding was row 95, landed by 101.]"], ["C", "[2026-09-15: DONE at `2026-09-15-board-90-doc-residue-005`, bumpless. The §8 bare-form paragraph rewritten around the ruling sentence verbatim; §5.4 names `bale config hooks` and `--forget`; the apply parser description brought to the row-87 contract; TARBALL.md §1 names `response-<sid>.tar.gz`. Calls ratified: the echo's rendering quoted in §8; the tie refusal noting each path's session; \"modification time\" in help text with `st_mtime_ns` in BALE.md; the closing why-sentence kept. Registry: §7/§7.2 includes-as-scope closed on the worker's read. Proposals: the positional's two words landed at 101; the `bale config hooks` command-table row rides 99b.]"], ["C", "[2026-09-15: DONE by `2026-09-15-board-101-bare-apply-cap-007` — the desk guess held (the read-includes order is 73b's intended routing) with one addition: the read-side refusal's remedies still spoke of `--write`, so the remedy was split by which rule fired in `bin/bale_report.py`; the empty-plan case pins the inherited-forecast contract. Dispatched to 101 as a rider before the desk recognized this row held it (§6 entry 137).]"], ["D", "New, ratified 2026-09-15 (the continue-plan-004 sitting; each a ruling of record with one home):"], ["D", "- **Bare apply is bounded by name and count.** Only files named `response-*.tar.gz` are ever opened; the two newest by `st_mtime_ns` are examined (one cutoff, so a tie at either rank is examined in full and refuses as before); resolution is among the examined files only, newest candidate wins; the no-candidate refusal names each examined file and its reason. Fixed at two by operator ruling, no config key. Supersedes the scan clause of board 51 only; row 87's content discrimination, tie refusal, echo, and non-TTY decline stand. Landed at row 101. - **A composed line carries every typed admission value.** Typed values as the gate normalized them first, then the refused or drifted ones, deduplicated per flag — one rule for the drift, required-check, and base-drift lines; a dropped flag re-sends the operator through a gate they already cleared. Landed at row 89. - **Every admission y/N names its decline cause.** Stdin closed, empty answer at a decline default, and an explicit answer each render a distinct literal line; the wizard's flow prompts are not admission gates. Landed at row 89 for the five non-hook prompts; the hook prompt landed it at row 83. - **A re-attempt ends with the quoted retry line.** Any response with `corrects` set ends with one paste-ready physical line, `bale retry \"<delivery dir>/response-<sid>.tar.gz\"`, quotes always, because a second download of the same name gains a `(1)`; the desk's brief names the delivery directory (a §7 fact) so the worker never invents a path. Row 102 makes the bare filename resolve on its own."], ["E", "136. **A row that slid out of the sequence.** Row 47 was sequenced ahead of the held wave with row 74 riding its doc touch; when row 74 was decoupled at the 09-14 close, the sequencing line was rewritten without 47 and no reader noticed until the operator asked, a sitting later, whether the HOLD paste block was still queued. A decoupling edits two places — the carrier row and the sequence — and the close checks both. (Desk-side, 2026-09-14/15.)"], ["E", "137. **The board consulted for riders, but by registry only.** Board 89's notes reported two handoff-gate test failures; the desk read them as a fresh finding, ruled on them, and dispatched them to 101 as a rider — while row 95 already held them verbatim from board 94, with a scope guess the ruling confirmed. The registry check at dispatch (entry 135) is not enough: a finding a worker surfaces may already be a row, and a rider's touched file may be an open row's home. The dispatch check now reads §4's open rows by touched file. (Desk-side, this sitting.)"], ["E", "138. **Rehearsing only the branch the sandbox can produce.** Board 101's first attempt was HELD with the blind checkpoint seven of seven and its own `validation.sh` exiting 1: the script grepped `validate.sh`'s output for a pass wording it had never seen, because `README.md` is not shipped in a request and so the sandbox only ever produced the one-failure branch. Practice residue: a needle against a shipped script's output is rehearsed against that script's pass branch or asserts its exit code; a branch the sandbox cannot reach is a branch the worker has not tested. The re-attempt's change set was byte-identical. (Worker-side, board 101.)"], ["E", "139. **Third light-tier specimen.** Board 89's worker asked one non-blocking wording question in chat before building, took the operator's \"go\" as no preference, kept its named default, and recorded the aside in notes.md as a detail call rather than an intent gap. Under the docs as shipped there is no tier for that question; under the 09-15 ruling there is, and the worker's reading matched it. Row 96 lands the tier; this is its evidence. (Worker-side, board 89.)"], ["E", "140. **\"Tests ship with code\" without forecasting tests.** Board 89's brief said tests ship with code and its forecast named five `bin/` paths and no `tests/` entry; seven test files were admitted out-of-forecast at apply. The fixture-consumer include rule (entry 133) covers the read side; the forecast side needs the same: a code row that will change or add tests forecasts `tests`. Rows 101's pack did. (Desk-side, this sitting.)"], ["F", "- Version landmark 0.4.32 (row 101, 2026-09-15); 0.4.31 at row 89 the same day. - Forecast rule: a code row that will change or add tests forecasts `tests` (entry 140); the include rule for fixture consumers (entry 133) is its read-side twin. - Re-attempt closing line: the desk's brief names the delivery directory — `/mnt/c/Users/chord/Downloads/` — and any response with `corrects` set ends with `bale retry \"<delivery dir>/response-<sid>.tar.gz\"`, quotes always (§5, 2026-09-15). - Dispatch check, grown: before emitting a bundle the desk reads the registry (entry 135) and §4's open rows by touched file (entry 137); a decoupling edits the carrier row and the sequencing line both (entry 136). - Bare-apply cost mechanism, for the record: `_peek_bare_candidate` reads a whole gzip stream per file (`getmembers()`); the resolver bounds how many files reach it (row 101). A directory of many tarballs is now two opens per bare run."], ["G", "- `bale config hooks` row in BALE.md §5's command table, and one sentence in BALE.md's apply and pack sections that every admission y/N names its decline cause — both ride 99b (the BALE.md true-up). From boards 90 and 89, 2026-09-15. - `bin/bale_apply.py`'s module docstring says the module never imports `bale_pack`; a lazy `import bale_pack` (~line 1144) contradicts it. Drop the sentence or lift the import — rides the next `bale_apply.py` touch. From board 101. - `shlex.quote` on the non-TTY bare-apply refusal's `bale apply {path}` line, matching the decline's quoted alternative — rides row 102. From board 101. - \"request includes\" in place of \"pack includes\" in the shared blindness diagnosis, so it reads right on a handoff — only if the byte-shared-diagnosis constraint is re-ratified; otherwise stands. From board 101."], ["G", "[2026-09-15: consumed at `2026-09-15-board-89-decline-cause-006`.]"], ["G", "[2026-09-15: closed at `2026-09-15-board-90-doc-residue-005` — §7.2's include bullets already carry ADR-0015's read-set language; nothing to fix.]"]]
fails=0
def check(label, ok):
    global fails
    print(("[PASS] " if ok else "[FAIL] ")+label); fails += 0 if ok else 1
check("header: Last landed by names this session, once",
      len(re.findall(r"Last landed by: `"+SID+r"`\.", c))==1)
check("header: the close-8 last-landed line is gone",
      "Last landed by: `2026-09-15-sitting-close-deltas-8-003`." not in c)
for h in ("## 1. Ultimate goal","## 2. Milestones","## 3. In flight","## 4. The board",
          "## 5. Contracts established","## 6. Orchestration-doctrine evidence pile",
          "## 7. Standing environment facts","## 8. Foundation-audit findings register"):
    check(f"skeleton: {h}", h in doc)
for name,p in pins:
    pat=re.escape(p).replace(re.escape("<your-sid>"), SID)
    n=len(re.findall(pat, c))
    check(f"block {name}: landed once — {p[:60]}…", n==1)
print(f"checkpoint: {len(pins)+10-fails}/{len(pins)+10} outcomes hold")
sys.exit(1 if fails else 0)
PY
