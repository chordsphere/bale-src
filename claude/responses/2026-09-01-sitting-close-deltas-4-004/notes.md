# notes — 2026-09-01-sitting-close-deltas-4-004

Every brief item landed; pinned tokens and quoted texts are
byte-verbatim (each pin on one unbroken line, per the wrapped-grep
lesson), and everything else is in compressed close style. The calls
below are the latitude the brief left me — flag-everything applies.

## Judgment calls to ratify

**The row-44 strike's extent.** The brief said to strike "the stale
'until it lands, every stats run warns' bracket." I read that as the
whole first 2026-08-31 fold-in bracket on row 44 (the one whose
forward-looking half was that warning), not just the one sentence —
its acceptance facts are re-recorded by the new true-up bracket
("opened" in IN_FLIGHT_OUTCOMES, provenance-stamp-first resolution,
first-customer status), so no history is lost, and the true-up
bracket names the strike explicitly as the close's one sanctioned
retroactive edit. If you wanted only the stale sentence removed and
the acceptance bracket kept, say so and I'll re-spin.

**True-up placement: row-44 vicinity, not §5.** The brief offered
either. The true-up is a verification fact about a landing, not a
contract, so it sits as a dated bracket on the row, beside the DONE
bracket it corrects.

**Tolerance-note placement: §7.** The brief offered §7 or "beside
the telemetry facts." I put it in §7 as a standing, dated
environment/corpus fact — that section is where a future reader
checks what the corpus tolerates, and the §5 ruling entry can point
at it without duplicating it. The `open_telemetry` pin also appears
naturally in row 67's annotation.

**A row-37 reshape bracket, unasked.** The ruling's second sentence
reshapes board 37, and the sequencing rider says "37 now packable."
A row 37 without any marker would send the next desk to a stale
three-part scope, so I bracketed a compressed pointer at the §5 text
of record onto the row. Pointer only — the ruling has one home.

**§7 version landmark trued up to 0.4.24.** The brief pinned
v0.4.23 (row 41) and v0.4.24 (row 42) onto the rows; §2 says the
current version has one home, §7's bin/bale landmark, which still
read 0.4.22. Leaving it stale while the rows say otherwise seemed
like the exact drift the one-home rule exists to prevent, so I moved
the landmark to 0.4.24 at 003, with 0.4.23's carrier in the
parenthetical. Strike if the desk tracks this differently.

**First specimen annotated on the §3 forecast-refusal watch.** The
brief routed the 67-r1 refusal datum to §6 (entry 103). Sibling
watches accrue their specimens in place (the forecast-precision
watch's four accruals are the model), so I also appended a one-line
first-specimen accrual to the watch entry, cross-pointing at entry
103. Record-keeping symmetry, not new content.

**Naming the sitting.** I could not verify the master session's full
sid from the shipped context, so the new §3 Landed block, brackets,
and evidence header reference it as "the continue-plan sitting
(session 026)" — the doc's own NNN-qualified-by-sitting convention —
rather than inventing a full sid. If you want the full sid recorded,
one edit to the Landed block's header does it.

**Evidence 100's continuation indent.** Three-digit entries are new
to §6; I kept the file's four-space continuation indent rather than
inventing a five-space variant. The pinned phrase inside entry 100
sits on its own unbroken line regardless.

## Claims

All six claims are annotated `observed`: validation.sh was rehearsed
in the build environment against the staged bytes (all pass) and
against the unmodified base (all five content checks correctly
fail) — the negative-run assertion discipline from the bin-bale-tidy
precedent.

No out-of-forecast paths: the single changes[] entry is
claude/MASTER.md, the forecast's one path.
