# notes — 2026-08-31-sitting-close-deltas-2-019

Doc-only close, one file. Everything from the brief's three items
landed; four judgment calls and two small flags below. Validation's
close assertions were also run negatively against the unedited doc
(17 FAILs) to confirm they bite, not just pass.

## Judgment calls to ratify

1. **Proposal-count attribution in the brief.** Item 2 says "five
   proposals from 016/017/018 were routed" and then lists five
   wave-3 destinations including the guard deny-shapes session —
   but the deny-shapes proposal is 015's (board 66's session), not
   016/017/018's. The counts do reconcile as five-from-016/017/018
   (016: the crafter citation; 017: the TARBALL.md mention and the
   status hint; 018: cmd_handoff and the stats read side) plus
   015's guard proposal as a sixth routing to the fifth
   destination. The doc's wave-2 dispositions block records all
   six routings with their source sessions named and makes no
   count claim, so the record is accurate whichever way the brief
   meant it. Say so if you'd rather the block mirror the brief's
   phrasing instead.

2. **The routing record's home is a new §3 block.** The routings
   have no row home and aren't fold-ins (their carriers are open
   sessions, not future touches), so I followed the
   exchange-arc-close precedent — a dated §3 block with a
   Proposals-dispositions bullet — rather than stretching the
   registry or scattering the routings across the five row
   brackets. The block states explicitly that it carries only what
   has no row home, so it doesn't read as a return to per-sitting
   narrative accretion. Wave 1 has no such block (the first close
   recorded everything on rows and in §5); if the asymmetry
   bothers the desk, the block's content could be folded elsewhere
   at the next close.

3. **One clause beyond the brief's letter, on row 59's bracket.**
   The row's queued text carried a live conditional — the crafter
   index-header rider "folds in here if this session's forecast
   gains tools/, else it runs as its own micro-session" — and 014's
   forecast never gained tools/. I recorded the resolution on the
   bracket (rides the wave-3 crafter pair) so the conditional
   doesn't dangle for the next reader. Strike the sentence if you
   want brackets confined strictly to the brief's given facts.

4. **The row-66 DONE bracket sits after the existing
   sweep-extension bracket**, in chronological bracket order (the
   §2 accretion convention), and notes that the ratified sweep
   extension executed — 015's judgment call 2 (sitting labels
   became version anchors) is that execution.

## Flags, no action taken

- **§7's version-position line still reads "bin/bale VERSION 0.4.17
  at `2026-08-26-board-53-amend-checkpoint-004`".** Item 3 says
  record versions on the rows only, and the closes that landed
  0.4.18–0.4.20 also left this line, so I left it too — but §3
  calls §7 "the version position's one home", and it is now five
  bumps stale. If the desk wants it moved to 0.4.22, it's a
  one-line edit (also in the manifest's deferred list).

- **Board 44's open forecast likely fires a queued registry
  entry.** The wave-3 board-44 sibling forecasts
  tests/test_stats_aggregation.py and the stats corpus — exactly
  the carrier the "Post-epoch stats-corpus fixtures" registry
  entry (and its 010 linkage-shapes extension) rides. Nothing to
  do here; just noting so the next close checks whether that entry
  was consumed alongside the row-44 landing.

## Verbatim-quote provenance

The two new registry entries quote 018's notes from the shipped
archive copy at
`context/claude/responses/2026-08-31-board-63-provenance-at-open-018/notes.md`
(the brief's primary source; the desk-paste fallback wasn't
needed). The normalize-at-stamp quote comes from the notes'
"Decisions to ratify" rider bullet, not the Proposals section —
that's where 018 recorded it, and the entry says so.
