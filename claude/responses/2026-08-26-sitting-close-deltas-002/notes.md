# notes — 2026-08-26-sitting-close-deltas-002

Straightforward landing; the crib was precise and the manifest
constraint resolved the one open call. Three judgment calls to
ratify, one convention edit outside the goal's three named delta
groups, and nothing surprising.

## Judgment calls

- **The escalation-contract-producer routing has no sub-item to
  annotate.** The crib says to annotate "each" of the three routed
  concerns (producer, clarification-relay subsumption,
  session-interaction mandate) with the seed-doc pointer. The
  latter two exist as board-10 sub-items and got their own
  `[2026-08-25: routes to the harness project — seed D13, D14]`
  annotations. The producer exists only as a parenthetical in §3
  ("the producer is S6's") and in the row's charter line, so its
  routing is carried in the row-level S6-discharge annotation,
  which names all three and points at the two item-level
  annotations. If you'd rather the §3 in-flight line also carry a
  pointer, that's a one-line follow-up; I left §3 untouched since
  it already defers to the board row for the S6 close.
- **Board 57 is queued neutral on sequencing.** The constraint
  ratifies it as its own small board item alongside 56. The crib's
  original "could ride the changelog session as the same small
  carrier" option reads as superseded by that ratification, so the
  item says "sequencing among the smalls is the next desk's call"
  rather than pre-suggesting the fusion. Correct me if the
  ride-along option was meant to survive as a recorded note.
- **The §5 changelog entry cross-references board 56/57
  concretely** where the crib's draft said "(below)" — the crib
  text was written before the item numbers existed; the landed
  entry names them so the pointer resolves.

## Convention edit outside the three named delta groups

- The header's `Last landed by:` line is edited in place to this
  sid. Not one of the goal's three delta groups, but the doc's own
  header convention says every landing edits it; skipping it would
  leave the header claiming the previous landing. In-forecast
  (`claude/MASTER.md` is the whole declared scope).

## Look closely

- The S6-discharge annotation sits after the "Remaining: S6 only"
  sentence rather than rewriting it — the row's standing pattern
  (dated bracketed annotations, headings and prior text untouched).
  The row heading's status phrase is likewise unchanged since the
  row stays open (sandbox-validation implementation half).
- "Substantially DISCHARGED" is the crib's wording, kept verbatim —
  deliberately not "DONE", since three concerns route onward to the
  harness project.
